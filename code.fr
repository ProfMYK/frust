let width = 800.0;
let height = 600.0;
init(as_integer(width), as_integer(height), "GAME");

let player = vec2(width / 2.0, height / 2.0);
let player_color = color(240, 50, 50);

while (!should_close()) {
  clear(color(18, 18, 18));
  circle(player, 16.0, player_color);
}
